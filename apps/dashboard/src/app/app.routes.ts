import { ReactWidgetComponent  } from './react-widget.component';
import { Route } from '@angular/router';

export const appRoutes: Route[] = [
  {
    path: '',
    component: ReactWidgetComponent ,
  },
];
